Flask==3.0.0
Flask-CORS==4.0.0
torch==2.1.0
torchvision==0.16.0
pydicom==2.4.4
pylibjpeg>=1.4.0
pylibjpeg-libjpeg>=1.3.0
pylibjpeg-openjpeg>=1.3.0
Pillow==10.1.0
opencv-python-headless==4.8.1.78
ultralytics==8.0.228
timm==0.9.12
numpy==1.24.3
Werkzeug==3.0.1
gunicorn==21.2.0
# Remove heavy dependencies not needed for inference
# matplotlib  # Only needed for YOLO visualization (which we do manually)
# seaborn  # Not needed for inference
# pandas  # Not needed for inference
